language: c
sudo: false
dist: trusty

os:
  - windows
  - linux
  - osx

compiler:
  - clang
  - gcc
  
script:
  - mkdir Release
  - cd Release
  - cmake .. -DCMAKE_BUILD_TYPE=Release
  - cmake --build .
  - ctest -C Release .
